<UserControl x:Class="PathEditor.SuggestTextBoxPath"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PathEditor"
                          xmlns:props="clr-namespace:PathEditor.Properties"
             xmlns:support="clr-namespace:PathEditor.Views.Support"
             xmlns:uc="clr-namespace:PathEditor.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

    </UserControl.Resources>
    <Grid>
        
        <uc:AutoCompleteTextBox Grid.Column="0"  x:Name="txt" x:FieldModifier="public"
                    Style="{StaticResource PathTextBoxStyle}"
                    Text="{Binding Path=SelectedPathPart.Path, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                    AutoCompleteCommand="{Binding AutoComplete}" />
    </Grid>
</UserControl>
